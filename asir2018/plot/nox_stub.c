#include "ca.h"
#include "parse.h"
#include "ox.h"
#include "ifplot.h"

void initmarker(struct canvas *can,char *msg)
{
  (void)can;
  (void)msg;
}

void marker(struct canvas *can,int dir,int pos)
{
  (void)can;
  (void)dir;
  (void)pos;
}

void count_and_flush(void)
{
}

void flush(void)
{
}
